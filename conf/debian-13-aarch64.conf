


# See conf/debian-13.conf for why the "generic" variant is used.
VM_VHD_LINK="https://cloud.debian.org/images/cloud/trixie/latest/debian-13-generic-arm64.qcow2"
VM_OS_NAME="debian"
VM_SSH_PORT=2222
VM_RELEASE=13

VM_ROOT_PASSWORD=anyvm.org

VM_LOGIN_TAG="login:"

VM_SHUTDOWN_CMD="shutdown -h now"

VM_INSTALL_CMD="DEBIAN_FRONTEND=noninteractive apt-get install -y "

VM_RSYNC_PKG="rsync"
VM_SSHFS_PKG="sshfs"

VM_PRE_INSTALL_PKGS="tree $VM_RSYNC_PKG $VM_SSHFS_PKG nfs-common"

VM_INSTALL_SCRIPT="hooks/vm_installpkgs.sh"

VM_ARCH=aarch64
VM_NIC=virtio

VM_USE_CONSOLE_BUILD=1
VM_NO_VNC_BUILD=1

VM_SYNC_METHODS="rsync,scp,sshfs,nfs,tar"
